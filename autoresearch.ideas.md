- The remaining gap is now mostly cold-start overhead: current measurements are roughly ~216 ms cold vs ~120 ms warm. Investigate whether any further meaningful gain can come from reducing shell/process startup before popup launch, rather than from more helper-script micro-optimizations.
- If pursuing more speed, focus on architecture-level changes only if they preserve behavior and are validated carefully in the harness (for example, reducing process boundaries on the launch path), because the straightforward shell-level micro-optimizations tried so far have mostly regressed.
